import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["localhost", "127.0.0.1", "kairetribe.com", "www.kairetribe.com","images.unsplash.com"],
  },
};

export default nextConfig;
